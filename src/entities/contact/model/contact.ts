export interface Contact {
    id: string;
    email: string;
    github: string;
    linkedin: string;
}

export interface ContactRequestModel {
    email: string;
    github: string;
    linkedin: string;
}